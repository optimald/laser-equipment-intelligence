{
  "name": "laser-equipment-intelligence-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "cd web && npm run dev",
    "start": "cd web && npm run start",
    "lint": "cd web && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
