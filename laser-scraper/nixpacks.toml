[phases.setup]
nixPkgs = ["python39", "python39Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -c 'import uvicorn; uvicorn.run(\"api.main:app\", host=\"0.0.0.0\", port=int(\"$PORT\"))'"